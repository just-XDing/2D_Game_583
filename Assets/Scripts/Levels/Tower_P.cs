using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tower_P : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Game Object Start");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
